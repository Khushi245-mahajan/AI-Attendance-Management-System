Project Context & GoalYou are building a production-ready Attendance Management System via a Streamlit Web Application written in Python 3.10+. The application must track attendance from uploaded video files (similar to a security camera clip or high-throughput crowd recording) or individual image uploads, matching individuals against a registered student database.Core Tech Stack (Non-Negotiable)UI/Frontend Framework: StreamlitComputer Vision Pipeline: insightface (using the buffalo_l ONNX model package for detection, 5-point landmark extraction, and 512-dimensional ArcFace embedding generation).Backend & Vector Storage: Supabase (PostgreSQL) leveraging the pgvector extension for server-side 512D vector cosine similarity operations.Image Processing Matrix Engine: opencv-python-headless and numpy.Technical Specifications & Guardrails1. Database Schema & Vector Search ConfigurationYou must configure the Supabase PostgreSQL backend using the exact SQL DDL below. The vector distance math must happen on the database side using cosine distance (<=>).SQL-- Core Extensions
CREATE EXTENSION IF NOT EXISTS vector;

-- Registry Table
CREATE TABLE IF NOT EXISTS students (
    student_id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    embedding vector(512),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, now()) NOT NULL
);

-- Attendance Logs
CREATE TABLE IF NOT EXISTS attendance_logs (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    student_id TEXT REFERENCES students(student_id),
    marked_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, now()) NOT NULL
);

-- Server-Side Vector Matching Function
CREATE OR REPLACE FUNCTION match_faces (
  query_embedding vector(512),
  match_threshold FLOAT,
  match_count INT
)
RETURNS TABLE (
  student_id TEXT,
  name TEXT,
  similarity FLOAT
)
LANGUAGE sql STABLE
AS $$
  SELECT
    students.student_id,
    students.name,
    1 - (students.embedding <=> query_embedding) AS similarity
  FROM students
  WHERE 1 - (students.embedding <=> query_embedding) > match_threshold
  ORDER BY students.embedding <=> query_embedding ASC
  LIMIT match_count;
$$;
2. High-Accuracy Preprocessing & Affine Alignment RuleTo guarantee embedding stability and prevent geometric variance across video frames, you must not pass a raw bounding box crop to the embedding engine. You must extract the 5-point facial landmarks and execute a similarity transformation matrix to warp the face into a canonical coordinate space.Target Size: $112 \times 112$ pixels (InsightFace canonical standard).Reference Coordinate Geometry (Warp Targets):Pythonreference_pts = np.array([
    [30.2946, 51.6963],  # Left Eye
    [65.5318, 51.5014],  # Right Eye
    [48.0252, 71.7366],  # Nose Tip
    [33.5493, 92.3655],  # Left Mouth Corner
    [62.7299, 92.2041]   # Right Mouth Corner
], dtype=np.float32)
Transformation Function: Use cv2.estimateAffinePartial2D(src_pts, reference_pts) followed by cv2.warpAffine() to generate a deterministic, translation-invariant face crop.3. Video Processing Performance RequirementsTo handle multi-person tracking within an uploaded video file without hanging the Streamlit execution thread:Frame Skipping: Do not process all 30 frames per second. Implement a configurable step parameter (e.g., sample every 5th or 10th frame) using cap.set(cv2.CAP_PROP_POS_FRAMES, frame_idx).In-Memory Session Caching: Once a student has been identified and verified above the confidence threshold ($0.65$ for ArcFace) during a video run, store their ID in a local Python set() to prevent redundant database hits or duplicate logs for the same session.Functional Requirements (The Application Code Layout)Generate a single or modular python script structure (app.py, aligner.py, database.py) implementing the following multi-tab dashboard:Initialization StepImplement st.session_state management to initialize the insightface.app.FaceAnalysis(name='buffalo_l') model exactly once. Force CPU execution provider unless specified. Ensure secrets are safely read using st.secrets["SUPABASE_URL"] and st.secrets["SUPABASE_KEY"].Tab 1: Registration/OnboardingInputs: Text inputs for Student ID and Full Name. A file uploader or camera input for an image file.Logic: Run detection $\rightarrow$ Extract landmarks $\rightarrow$ Execute Affine Warp alignment $\rightarrow$ Forward to embedding engine $\rightarrow$ cast numpy.float32 vector array directly to a standard Python list $\rightarrow$ Insert into Supabase table students.UI Feedback: Render the final aligned $112 \times 112$ crop back to the user with an st.success() toast confirming DB persistence.Tab 2: High-Throughput Attendance ProcessingInputs: An st.file_uploader restricting input to video formats (.mp4, .avi, .mov). An interactive slider for the matching confidence threshold (defaulting to 0.65).Logic: Instantiate a loop reading the file via cv2.VideoCapture. On sampled frames, extract multiple faces, loop through each detected face landmark array, align the crop, generate the embedding, and trigger the supabase.rpc("match_faces", ...) payload.Logging: If a match returns valid data, write an immediate row into attendance_logs containing the matching student_id.UI Feedback: Render a progress bar tracking frame execution. When finished, display a clean interactive st.dataframe() displaying the records compiled from a join of the attendance_logs and students tables for that day, complete with an st.download_button() exporting a CSV spreadsheet.Code Quality InstructionsNo placeholders: Generate all code completely. Write explicit try-except blocks around network payloads and model calls.Strict types: Provide explicit data type conversions, especially when handling data mapping between numpy arrays and Supabase JSON payloads.Scannable Layout: Ensure clean separation of concerns between raw image transformations, data persistence modules, and the core rendering layer.Please generate the code following these architectural instructions now.



build the project like a senior dev , ensure the archtitecture is modular , easy to debug for ai and human, add feedback for user with proper message so we can understand what exactly whent wrong and why